[Service]
# El análisis OCR/IA puede tardar varios minutos en CPU. Gunicorn mantiene
# disponible un margen amplio sin cambiar la cantidad actual de workers.
Environment="GUNICORN_CMD_ARGS=--timeout 600 --graceful-timeout 30"
