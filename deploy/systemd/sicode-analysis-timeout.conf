[Service]
# Los análisis nuevos de SICODE.IA se ejecutan en RQ y ya no bloquean Gunicorn.
# Se conserva un margen amplio para rutas documentales históricas/síncronas.
Environment="GUNICORN_CMD_ARGS=--timeout 1200 --graceful-timeout 45"
