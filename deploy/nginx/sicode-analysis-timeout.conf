# Este archivo se incluye dentro del bloque http de Nginx en CentOS Stream.
# No define client_max_body_size para no duplicar sicode_upload.conf si ya existe.
proxy_connect_timeout 60s;
proxy_send_timeout 600s;
proxy_read_timeout 600s;
send_timeout 600s;
